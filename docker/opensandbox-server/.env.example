# OpenSandbox Server 衍生镜像名称；通常保持默认，或改成内部镜像仓库地址。
OPENSANDBOX_SERVER_IMAGE=aos/opensandbox-server:latest
# OpenSandbox Server 对外提供 HTTP API 的宿主机端口；Cluster 注册时使用该端口。
OPENSANDBOX_SERVER_PORT=8090
# DinD 模式下映射给沙盒使用的连续端口范围；必须与 sandbox.toml 中的值一致。
SANDBOX_PORT_MIN=10000
SANDBOX_PORT_MAX=10100
