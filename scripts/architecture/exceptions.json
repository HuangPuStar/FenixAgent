{
  "_comment": [
    "架构例外台账：登记存量边界违规，精确到「规则 + 来源包 + 目标包」，每条带所属任务号与移除条件。",
    "门禁（scripts/check-dependency-boundaries.ts）只阻断未登记的**新增**违规，并在某条不再违规时报错要求删除。",
    "owner 取值：在排任务的编号（如 1.6 / 1.7）或「未排期」——后者表示该违规不属于任何在排任务的范围，",
    "是本次门禁修复后被如实暴露出来的既有债务。逐包核查结论与裁定记录在各任务的 review 文档里：",
    "docs/design/ce-ee-refactoring/review/ 下的 task-1.2-platform-identity-authorization.md（身份与授权）、",
    "task-1.3-resource-packages.md（资源包）、task-1.4-agent-runtime.md（agent-runtime 与 machine/sandbox 方向收敛）、",
    "task-1.5-host-aggregation.md（宿主聚合与本次台账据实改写）。",
    "禁止把 from / to 写成通配（加载器会直接拒绝）：通配会让整类边界静默失效。",
    "【环的代表边与 owner 归属】no-circular 的粒度为「每环一条」：环上每条边不逐条上报，from / to 只是该环上被挑中的一条",
    "**真实边**（相邻两点），不是环的参与者全集。被挑中的边若落在包内，包对指纹就会把跨包环显示成包内自环",
    "（`@fenix/resource-machine → @fenix/resource-machine` 这一指纹即如此，本族在该指纹下的 10 处全是跨包环）。归属一律看",
    "cycle 数组里的参与者包集合，不看 from / to 的包名；环长 = cycle 数组长度（环上节点数）。代表边不稳定：别的改动收窄",
    "装配面后，代表边会在包对之间迁移、指纹计数随之外观变化，环本身不变——所以「删掉某条指纹下的条目」不等于环被消解，",
    "各条 removeWhen 只写真正闭合该环的那条边。",
    "【全局口径，2026-09-21 1.5g 复测】工作树 872010416、2411 modules、按 `check:dependencies` 的参数重跑：上报 48 条环、",
    "参与环的边 84 条。其中 4 包环族（agent-config / agent-runtime / resource-machine / resource-sandbox）实测 38 处、",
    "分布在 6 个包对指纹下、环长 11–17，每条环各含一次固定的 4 条跨包边（machine → agent-config、agent-config → agent-runtime、",
    "agent-runtime → sandbox、sandbox → machine），共同闭合边是 §2.3 矩阵外的 machine → agent-config。此前各条尾注里的 223 / 161",
    "（更早口径）与 79 / 47（W6b-3 口径）至此统一：1.4 §21.6 登记过「各 owner 或最终收口统一」，本段即那次收口。",
    "各条 rationale 里的家族级数字是各自改写时点的快照（如 machine → machine 条目仍写「族内总数 37 处」，那是 W6b-3 时点的实测，",
    "本次实测为 38 处）；快照与本段不一致时以本段与届时实测为准，各条应在自己的 owner 任务改写时同步。",
    "【4 包环族 owner 改挂「未排期」，2026-09-21 用户裁定】agent-config / agent-runtime / resource-machine / resource-sandbox 这 6 个",
    "no-circular 指纹（machine → agent-config、machine → machine、sandbox → machine、sandbox → sandbox、agent-runtime → agent-runtime、",
    "agent-config → agent-config）的删除条件全部挂在同一条边上——machine → agent-config（§2.3 矩阵外的反向边，其三条未触发风险与",
    "两条候选修复路径见该条 rationale 末段）——而该边没有任何在排分片承接，原 owner 1.4 已结项（1.4 §22.3 已把这族明确推出）。",
    "故按用户裁定统一改挂「未排期」（不假手已结项任务），待 machine 包的方向收敛任务立项时改挂它的编号；届时删掉",
    "remote-file-service.ts:1 那一行 import 即可一次清掉 6 条。",
    "owner 必须指向**真正承接**该违规的排期任务，或显式的「未排期」；**不得保留已结项任务号**——那会让读表人误以为有人在管（2026-09-21 按用户裁定清理过一次：1.2 / 1.4 / 1.5 三个号当时清零，改挂 1.7 或「未排期」）。"
  ],
  "exceptions": [
    {
      "rule": "no-circular",
      "from": "@fenix/agent-runtime",
      "to": "@fenix/agent-runtime",
      "owner": "未排期",
      "removeWhen": "machine 删除 §2.3 依赖矩阵外的 `machine → agent-config` 反向边（本族 38 处环的共同闭合边，每条环各含一次）——该边一删，本条与其余 5 个指纹（1.4 名下 4 条 + `@fenix/agent-config → @fenix/agent-config`）一并消失。原先写的「1.5 下沉 apps/server 能力」已不是本条的移除条件：原 32 处环**每条都含** `@fenix/server-app → @fenix/agent-runtime`（32 处），该边随 1.5b / 1.5c 归零后本条降到 14 处，剩余环里已无任何宿主参与者。",
      "rationale": "1.5g 复测（2026-09-21，工作树 872010416，2411 modules，按 `check:dependencies` 的参数重跑）据实改写：实测 **14 处环（环长 14–17）**；上次记载「32 处环（环长 3–9）」，处数与环长均不符。参与包只剩一族：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（14 处）。上次记载的三支里，含 @fenix/server-app 的两支（27 处 + 3 处）与含 @fenix/resource-knowledge 的第三支（2 处）已随 1.5b / 1.5c 的包 → 宿主边下沉整体归零；本条 `from` / `to` 落在 agent-runtime 包内边，是代表边分布的结果，不是包内自环。环上跨包边已同质化为固定 4 条、每条环各含一次：@fenix/agent-runtime → @fenix/resource-sandbox、@fenix/agent-config → @fenix/agent-runtime、@fenix/resource-machine → @fenix/agent-config、@fenix/resource-sandbox → @fenix/resource-machine（本指纹下各 14 处）。根因随之改写：不再是「包 ↔ 应用倒置」，而是矩阵外的 @fenix/resource-machine → @fenix/agent-config 反向边闭合本族环；该边同时是 1.4 名下 4 条同族条目的共同闭合边，本族实测 38 处环、分布在 6 个包对指纹下，本条是其中之一。owner 保持 1.5：1.4 §22.3 把这族登记为 1.4 的未结项，归属写明「须在 §1.5 或 machine 包的独立任务中处理」，故本次不做改派（见 review/task-1.5-host-aggregation.md 的 1.5g【需审核】）。环的代表边语义、归属依据与上报粒度见本台账 `_comment` 的「环的代表边与 owner 归属」段。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.5）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/resource-machine",
      "to": "@fenix/agent-config",
      "owner": "未排期",
      "removeWhen": "machine 删除 machine → agent-config 反向边（W6b-3 实测这一族 37 处环每条都含该边一次，是本族环的共同闭合边）；machine → agent-runtime 反向边按 §五 的方向收敛目标一并删除（它在 W6b-3 实测中已不再出现在本族环上）；或本边经「改指窄入口（`./server/runtime` / `./server/config`）或改为宿主注入 lookup 端口」消失后一并删除（2026-09-21 记，见 rationale 末段）",
      "rationale": "W6b-3 交付后（abd24f9b0，2397 modules）实测 **1 处环（环长 13）**，与上次记载一致。参与包：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（1 处）。环上相邻的真实边各 1 条：@fenix/resource-machine → @fenix/agent-config（machine/services/remote-file-service.ts 读 agent_config 的 agentNode / machineId）、@fenix/agent-config →（dynamic import @fenix/agent-runtime/runtime） → @fenix/agent-runtime/services/orchestration-bootstrap → @fenix/resource-sandbox → @fenix/resource-machine。根因：§2.3 只登记 sandbox → machine、agent-config → agent-runtime 与 agent-runtime → sandbox/machine，环由矩阵外的 machine → agent-config 反向边闭合（同族环 W6b-3 实测 37 处，本条目 1 处）。代表边不稳定：W4 与 W6b 两次去掉若干 agent-runtime 装配面边后，这条环的代表边在包对之间反复迁移，环本身、总数与根因均未变化（W4a 的 20 → 19 台账收缩是同一现象）。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。\n\n**（1.5 结项复核补记，2026-09-21）本代表边对应的真实依赖是一行生产代码**：`packages/resources/machine/src/server/services/remote-file-service.ts:1` 的 `import { type AgentNode, getAgentConfigById, resolveAgentNode } from \"@fenix/agent-config/server\"`（第 61–62 行读 agent_config 行取 `agentNode` / `machineId` 以定位 machine）。它不只是账目问题，三条眼下未触发、已核实的风险：① `getAgentConfigById` 经 `getAgentConfigModule()` 读 agent-config 的**模块单例**，未装配即抛错（`packages/resources/agent-config/src/server/runtime.ts:25-30`，包内明示「不做兜底」），因此 **machine 的文件 API 可用性取决于 profile 是否启用 agent-config**——`ce.json` 恒启用才掩盖了这层依赖，最小 profile 或只装 machine 的集成测试会当场失败；这也正撞上 §2.3 对 machine「不得回调 repository、singleton 或生命周期实现」的措辞。② machine 静态导入的是 agent-config 的 **barrel**（`@fenix/agent-config/server`），连带把该包的 elysia 路由面与 `@fenix/agent-runtime/runtime`（经 `server/services/meta-agent.ts:14` 的静态 import）拉进 machine 的加载图，当前安全仅依赖 `ce.json` 的拓扑序恰好先装 agent-config，与 agent-config `./server/runtime` 注释里「宿主只要装配结果时不要经 `./server` barrel」的既有约定相悖。③ 本边与其余三条边构成环并已登记为例外，故新增同类反向边不再产生告警（可观测性损失，非功能损失）。\n\n**处置裁定（用户，2026-09-21）：本轮只记账、不修复**，修复留待后续任务；两条候选路径见 `removeWhen` 的加注。另注：`remote-file-service.ts:61` 的调用未传 `organizationId`，该路径不做归属校验，修复时需一并核对上游是否已按组织校验过该 environment。归属本条的 owner 仍是已结项的任务 1.4，尚无在排分片承接（见 1.5 结项记录 §8.3 与 1.5g-4 遗留）。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.4）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/agent-config",
      "to": "@fenix/agent-config",
      "owner": "未排期",
      "removeWhen": "同 `@fenix/agent-runtime → @fenix/agent-runtime` 一条：machine 删除矩阵外的 `machine → agent-config` 反向边后，本指纹的 3 处环与同族其余 35 处一并消失。原 removeWhen 的「1.5 下沉 apps/server 能力」已随 1.5c 交付，实测剩余 3 处环里已无任何 @fenix/server-app 参与者。",
      "rationale": "1.5g 复测（2026-09-21，工作树 872010416，2411 modules，按 `check:dependencies` 的参数重跑）据实改写：实测 **3 处环（环长 14–15）**；上次记载「10 处环（环长 8–12）」，处数与环长均不符。参与包只剩一族：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（3 处）。上次记载的「@fenix/agent-config ↔ @fenix/agent-runtime（7 处）」两包形态，以及含 @fenix/resource-skill / @fenix/server-app 的两支（2 处 + 1 处），实测均已不再出现在本指纹下——前者随 agent-config 取数面收敛消失，后者随 1.5b / 1.5c 的包 → 宿主边下沉归零。根因改写：agent-config → agent-runtime 本身是 §2.3 登记的合法方向，本指纹下的环同样由矩阵外的 @fenix/resource-machine → @fenix/agent-config 反向边闭合，与 1.4 名下 4 条同族（本族实测 38 处 / 6 指纹 / 环长 11–17）。owner 保持 1.5，理由同 `@fenix/agent-runtime → @fenix/agent-runtime` 一条（1.4 §22.3 未结项表；见 1.5g【需审核】）。环的代表边语义、归属依据与上报粒度见本台账 `_comment` 的「环的代表边与 owner 归属」段。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.5）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/resource-knowledge",
      "to": "@fenix/resource-knowledge",
      "owner": "未排期",
      "removeWhen": "包内入口自环消除：`src/server/services/agent-knowledge.ts` 不再以包名导入自身入口（改为相对导入，或 `src/server.ts` 不再 `export *` 该服务）即可。该自环由阶段 1 的 PHY-05 资源闭包迁移（0b4ec5858，2026-09-16）引入，早于阶段 2，不由任何阶段 2 任务消除。",
      "rationale": "1.5g 复测（2026-09-21，工作树 872010416，2411 modules，按 `check:dependencies` 的参数重跑）据实改写：实测 **1 处环（环长 2）**；上次记载「2 处环（环长 2–11）」，其中含 @fenix/server-app 的那 1 处已随 1.5 的包 → 宿主边下沉归零（本包现仅剩 1 处 `@server/db/schema` 表定义导入，见本台账同包的 apps-boundary 条目）。剩下的 1 处是**纯包内自环**：`src/server/services/agent-knowledge.ts:2` 以包名导入 `@fenix/resource-knowledge/server` 取 `agentKnowledgeBindingRepo` / `knowledgeBaseRepo`，而 `src/server.ts:20` 又 `export * from \"./server/services/agent-knowledge\"`——环长 2、无跨包边。该自引用由阶段 1 的 PHY-05 资源闭包迁移（0b4ec5858，2026-09-16）引入，早于阶段 2，与宿主聚合无关。owner 由 1.5 改派「未排期」：本台账对「未排期」的定义正是「不属于阶段 2 任何任务范围、如实暴露出来的既有债务」，且原 removeWhen 的前提（环内有指向 @fenix/server-app 的能力面）已随 1.5c 消失（见 1.5g【需审核】）。环的代表边语义、归属依据与上报粒度见本台账 `_comment` 的「环的代表边与 owner 归属」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/resource-machine",
      "to": "@fenix/resource-machine",
      "owner": "未排期",
      "removeWhen": "machine 删除 machine → agent-config 反向边（W6b-3 实测这一族 37 处环每条都含该边一次，是本族环的共同闭合边）",
      "rationale": "W6b-3 交付后（abd24f9b0，2397 modules）实测 **10 处环（环长 11–16）**；上次记载为「37 处环（环长 11–17）」，据实改写。参与包：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（10 处）。环上出现的跨包边恰好每条环各一次：@fenix/resource-machine → @fenix/agent-config（10 处）、@fenix/agent-config → @fenix/agent-runtime（10 处）、@fenix/agent-runtime → @fenix/resource-sandbox（10 处）、@fenix/resource-sandbox → @fenix/resource-machine（10 处）。根因：§2.3 只登记 sandbox → machine、agent-config → agent-runtime 与 agent-runtime → sandbox/machine，环由矩阵外的 machine → agent-config 反向边闭合。上次记载的 37 处是该族环在这一指纹下的上报数：W6b 收窄 agent-runtime 的装配面导出后代表边重新分布到 6 个包对（本条目 37 → 10 处），族内总数仍为 37 处、环长范围仍为 11–17，根因与参与包集合未变。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.4）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/resource-sandbox",
      "to": "@fenix/resource-machine",
      "owner": "未排期",
      "removeWhen": "machine 删除 machine → agent-config 反向边（W6b-3 实测这一族 37 处环每条都含该边一次，是本族环的共同闭合边）",
      "rationale": "W6b-3 交付后（abd24f9b0，2397 modules）实测 **3 处环（环长 12–15）**；上次记载为「3 处环（环长 14）」，处数一致、环长与实测不符，据实改写。参与包：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（3 处）。环上出现的跨包边恰好每条环各一次：@fenix/resource-machine → @fenix/agent-config（3 处）、@fenix/agent-config → @fenix/agent-runtime（3 处）、@fenix/agent-runtime → @fenix/resource-sandbox（3 处）、@fenix/resource-sandbox → @fenix/resource-machine（3 处）。根因：§2.3 只登记 sandbox → machine、agent-config → agent-runtime 与 agent-runtime → sandbox/machine，环由矩阵外的 machine → agent-config 反向边闭合。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.4）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/resource-sandbox",
      "to": "@fenix/resource-sandbox",
      "owner": "未排期",
      "removeWhen": "machine 删除 machine → agent-config 反向边（W6b-3 实测这一族 37 处环每条都含该边一次，是本族环的共同闭合边）",
      "rationale": "W6b-3 交付后（abd24f9b0，2397 modules）实测 **7 处环（环长 12–15）**；上次记载为「8 处环（环长 6–16）」，据实改写。参与包：@fenix/agent-config ↔ @fenix/agent-runtime ↔ @fenix/resource-machine ↔ @fenix/resource-sandbox（7 处）——上次记载的「@fenix/resource-machine ↔ @fenix/resource-sandbox（5 处）」这类两包形态在 W6b-3 实测中不再出现在本指纹下。环上出现的跨包边恰好每条环各一次：@fenix/resource-machine → @fenix/agent-config（7 处）、@fenix/agent-config → @fenix/agent-runtime（7 处）、@fenix/agent-runtime → @fenix/resource-sandbox（7 处）、@fenix/resource-sandbox → @fenix/resource-machine（7 处）。根因：§2.3 只登记 sandbox → machine、agent-config → agent-runtime 与 agent-runtime → sandbox/machine，环由矩阵外的 machine → agent-config 反向边闭合；上次记载的「由反向的 machine → sandbox 闭合」形态在本指纹下已不再出现。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。 owner 于 2026-09-21 按用户裁定改挂「未排期」（原 1.4）：本族 6 个指纹的删除条件都挂在同一条边上，理由见本台账 `_comment` 的「4 包环族 owner 改挂「未排期」」段。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/ccb",
      "to": "@fenix/ccb",
      "owner": "未排期",
      "removeWhen": "包内入口自环消除；跨 acp-link 的 3 处按用户裁定不改动",
      "rationale": "4 处环（环长 2–5）。参与包：@fenix/ccb ↔ acp-link（3 处）；@fenix/ccb（1 处）。环上出现的主要跨包边：@fenix/ccb → acp-link（3 处）、acp-link → @fenix/ccb（3 处）。根因：包内入口自环（handler 以包名导入自身根入口）或包内组合面自环；插件包 ↔ acp-link 双向依赖（acp-link 根入口值导入 handler，handler 反向导入 acp-link）。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/claude-code",
      "to": "@fenix/claude-code",
      "owner": "未排期",
      "removeWhen": "包内入口自环消除",
      "rationale": "1 处环（环长 2）。参与包：@fenix/claude-code（1 处）。环上出现的主要跨包边：。根因：包内入口自环（handler 以包名导入自身根入口）或包内组合面自环。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。"
    },
    {
      "rule": "no-circular",
      "from": "@fenix/opencode",
      "to": "@fenix/opencode",
      "owner": "未排期",
      "removeWhen": "包内入口自环消除；跨 acp-link 的 3 处按用户裁定不改动",
      "rationale": "4 处环（环长 2–5）。参与包：@fenix/opencode ↔ acp-link（3 处）；@fenix/opencode（1 处）。环上出现的主要跨包边：@fenix/opencode → acp-link（3 处）、acp-link → @fenix/opencode（3 处）。根因：包内入口自环（handler 以包名导入自身根入口）或包内组合面自环；插件包 ↔ acp-link 双向依赖（acp-link 根入口值导入 handler，handler 反向导入 acp-link）。环的代表边语义、归属依据、上报粒度与环长定义见本台账 `_comment` 的「环的代表边与 owner 归属」段：`from` / `to` 只是 dependency-cruiser 为该环挑出的代表边，不是参与者集合；归属看 `cycle` 数组的参与者包集合。"
    },
    {
      "rule": "undeclared-workspace-dependency",
      "from": "@fenix-agent/acp-runtime-cli",
      "to": "acp-link",
      "owner": "未排期",
      "removeWhen": "该包不再把 acp-link 作为构建期输入（改为 external 发布），或 acp-link 正式发布后改为普通 dependencies",
      "rationale": "实测 1 处导入 / 1 个文件。src/bin.ts 从 acp-link 根入口导入 startServer，但 package.json 只把它声明在 devDependencies。这在该包是正确声明：`bun build` 把 acp-link 连同其插件依赖打进单一产物 dist/bin.js（实测 dist/bin.js 内无裸 `acp-link` 导入），发布物自包含，消费者不需要 acp-link。把 `workspace:*` 提升为运行时 dependencies 会改变已发布 npm 包的依赖语义，超出 1.1 范围，故登记为豁免。此条由 1.1 修掉「只认 @fenix/ 前缀」的解析盲区后暴露。"
    },
    {
      "rule": "undeclared-workspace-dependency",
      "from": "acp-link",
      "to": "@fenix/ccb",
      "owner": "未排期",
      "removeWhen": "acp-link 转为 private（不再发布），或 @fenix/ccb 发布后把该边改为 dependencies",
      "rationale": "实测 1 处导入 / 1 个文件。acp-link 是仓库里两个未标 `private` 的包之一（另一个是 @fenix-agent/acp-runtime-cli，见同组上一条），`files` 直接发布 `src/`、`exports[\".\"]` 指向 src/server.ts 并在顶层值导入这三个 plugin 包（src/server.ts:5-7），因此这四条边在发布语义上确实存在。不把它们提升为 `dependencies` 的原因是四个目标包都是 `private: true` 的未发布包：提升后发布物会带上 `workspace:*` 依赖并指向 registry 上不存在的包——不是修好而是把「无法在 workspace 外运行」写进已发布清单。仓库内消费方（docker/sandbox*/Dockerfile 的`bun build` 独立 bundle、acp-runtime-cli 的 dist/bin.js）都从源码打包，devDependencies 已足够解析。改变已发布包的依赖语义超出 1.1 范围，故登记豁免。与 no-circular 里“@fenix/ccb / @fenix/opencode / @fenix/claude-code ↔ acp-link”的环条目是同一批边的声明面，故 owner 同为「未排期」。 导入点：src/server.ts:5 值导入 createCcbHandler。"
    },
    {
      "rule": "undeclared-workspace-dependency",
      "from": "acp-link",
      "to": "@fenix/claude-code",
      "owner": "未排期",
      "removeWhen": "acp-link 转为 private（不再发布），或 @fenix/claude-code 发布后把该边改为 dependencies",
      "rationale": "实测 1 处导入 / 1 个文件。acp-link 是仓库里两个未标 `private` 的包之一（另一个是 @fenix-agent/acp-runtime-cli，见同组上一条），`files` 直接发布 `src/`、`exports[\".\"]` 指向 src/server.ts 并在顶层值导入这三个 plugin 包（src/server.ts:5-7），因此这四条边在发布语义上确实存在。不把它们提升为 `dependencies` 的原因是四个目标包都是 `private: true` 的未发布包：提升后发布物会带上 `workspace:*` 依赖并指向 registry 上不存在的包——不是修好而是把「无法在 workspace 外运行」写进已发布清单。仓库内消费方（docker/sandbox*/Dockerfile 的`bun build` 独立 bundle、acp-runtime-cli 的 dist/bin.js）都从源码打包，devDependencies 已足够解析。改变已发布包的依赖语义超出 1.1 范围，故登记豁免。与 no-circular 里“@fenix/ccb / @fenix/opencode / @fenix/claude-code ↔ acp-link”的环条目是同一批边的声明面，故 owner 同为「未排期」。 导入点：src/server.ts:6 值导入 createClaudeCodeHandler。"
    },
    {
      "rule": "undeclared-workspace-dependency",
      "from": "acp-link",
      "to": "@fenix/opencode",
      "owner": "未排期",
      "removeWhen": "acp-link 转为 private（不再发布），或 @fenix/opencode 发布后把该边改为 dependencies",
      "rationale": "实测 1 处导入 / 1 个文件。acp-link 是仓库里两个未标 `private` 的包之一（另一个是 @fenix-agent/acp-runtime-cli，见同组上一条），`files` 直接发布 `src/`、`exports[\".\"]` 指向 src/server.ts 并在顶层值导入这三个 plugin 包（src/server.ts:5-7），因此这四条边在发布语义上确实存在。不把它们提升为 `dependencies` 的原因是四个目标包都是 `private: true` 的未发布包：提升后发布物会带上 `workspace:*` 依赖并指向 registry 上不存在的包——不是修好而是把「无法在 workspace 外运行」写进已发布清单。仓库内消费方（docker/sandbox*/Dockerfile 的`bun build` 独立 bundle、acp-runtime-cli 的 dist/bin.js）都从源码打包，devDependencies 已足够解析。改变已发布包的依赖语义超出 1.1 范围，故登记豁免。与 no-circular 里“@fenix/ccb / @fenix/opencode / @fenix/claude-code ↔ acp-link”的环条目是同一批边的声明面，故 owner 同为「未排期」。 导入点：src/server.ts:7 值导入 createOpencodeHandler。"
    },
    {
      "rule": "undeclared-workspace-dependency",
      "from": "acp-link",
      "to": "@fenix/plugin-sdk",
      "owner": "未排期",
      "removeWhen": "acp-link 转为 private（不再发布），或 @fenix/plugin-sdk 发布后把该边改为 dependencies",
      "rationale": "实测 2 处导入 / 2 个文件。acp-link 是仓库里两个未标 `private` 的包之一（另一个是 @fenix-agent/acp-runtime-cli，见同组上一条），`files` 直接发布 `src/`、`exports[\".\"]` 指向 src/server.ts 并在顶层值导入这三个 plugin 包（src/server.ts:5-7），因此这四条边在发布语义上确实存在。不把它们提升为 `dependencies` 的原因是四个目标包都是 `private: true` 的未发布包：提升后发布物会带上 `workspace:*` 依赖并指向 registry 上不存在的包——不是修好而是把「无法在 workspace 外运行」写进已发布清单。仓库内消费方（docker/sandbox*/Dockerfile 的`bun build` 独立 bundle、acp-runtime-cli 的 dist/bin.js）都从源码打包，devDependencies 已足够解析。改变已发布包的依赖语义超出 1.1 范围，故登记豁免。与 no-circular 里“@fenix/ccb / @fenix/opencode / @fenix/claude-code ↔ acp-link”的环条目是同一批边的声明面，故 owner 同为「未排期」。 导入点：src/server.ts:8 与 src/client/instance-manager.ts:5，两处均为 `import type`（AgentLaunchSpec）；后者位于已发布的 `./client` 子路径内。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/agent-runtime",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "§1.7 表定义迁出后本包不再引用 `@server/**`（1.5c-2 复测时非测试侧只剩 5 处 `@server/db/schema`，1.5g 复测不变），届时删除本条。原第②条路径（`orchestration-instance.ts` 的 `@server/config`）已在 1.4 收尾前后消失，见 rationale 的 1.5c-2 复测段。**注意本条的违规面含测试侧 18 处宿主测试基建耦合**（以 `@server/test-utils/stubs/module-stubs` 的 16 处为主，见 1.5g 复测段），仅完成 §1.7 表定义迁出不会让本条失效——`@server/**` 两个面都归零才可删除。任一条路径有进展或新增 `@server/**` 引用时按当时事实重测本条。",
      "rationale": "W4b-2 交付后（a0556aaee）实测 **6 处导入 / 6 文件**（非测试）：5 处 `@server/db/schema` 表定义（`server/repositories/{agent-instance,environment,environment-orchestration}.ts`、`server/services/environment-web.ts`、`services/agent-chat-service.ts`，按 §9.3 裁定归 §1.7）+ 1 处 `@server/config`（`services/orchestration-instance.ts` 的 `config.defaultEngineType` 与 `getBaseUrl()`）。**W4 只消除了 5 处、不是全部**：W2 交付后（2c57097f3）本条的 11 处 / 8 文件中，`launch-spec-builder.ts`（3 处）随 W4b 整文件删除、`orchestration-bootstrap.ts`（1 处 `agentEngineRepo`）随 §9.2 收敛删除，而 `orchestration-instance.ts` 的 `@server/config` **按 §15.3 明确留在本包**——`getBaseUrl()` 喂 `USER_META_BASE_URL`、`config.defaultEngineType` 决定本地执行引擎，两者都是「实例上下文」（与密钥同源），不属于被搬走的「取数」，因此原 removeWhen 把它记为「W4 消除」不成立，据实改写。W1 交付后基线 30 处 / 14 文件（27 值 + 3 类型），W2 消除 19 处 / 6 类；测试侧由 49 处 / 28 文件降为 30 处 / 23 文件（W2 后），再降到 **16 处 / 15 文件**（W4b-2 后；其中 `@server/test-utils/stubs/module-stubs` 12 处、`@server/config` 2、`@server/db/schema` 1、`@server/plugins/error-handler` 1，属宿主测试基建的既有耦合）。原 105 处 / 54 文件是 W2 动手前的全量口径（含测试与当时仍在宿主的模块导入）。按裁定「按职责面收敛 + 表定义显式豁免」保留本条，删除条件见 removeWhen。 W6b-3 交付后（abd24f9b0，2397 modules）复核：非测试侧仍为 6 处导入 / 6 文件（5 处 `@server/db/schema` + 1 处 `@server/config`），测试侧仍为 16 处 / 15 文件，两条 removeWhen 路径均未变化——W6b 只动了 agent-runtime 的导出面与跨包消费方，未触及 `@server/**` 导入。1.5c-2 复测（2026-09-21，工作树 d22212822 + 1.5c-2 改动）据实改写：非测试侧 **5 处 / 5 文件**，全部是 `@server/db/schema`（即上段列举的 5 个文件），`@server/config` 的 1 处（`orchestration-instance.ts`）随 `a084454c6`（「切断包到宿主的 Redis 与模块配置残留边」）消失——该文件现只从 `@fenix/logger`、`@fenix/orchestration`、`@fenix/platform-sdk`、`@fenix/plugin-sdk` 取值，removeWhen 的第②条路径因此不复存在；测试侧 **17 处 / 17 文件**（`@server/test-utils/stubs/module-stubs` 15、`@server/db/schema` 1、`@server/plugins/error-handler` 1），与上段记载的 16 处 / 15 文件相比，`@server/config` 的 2 处同批消失、`module-stubs` 由 12 处增至 15 处——新增的三处都来自 1.5c 迁入本包的控制台路由用例：控制面（`web-control-routes.test.ts`）与实例/环境路由（`instances-delete-idempotent.test.ts`、`round44-environments-routes.test.ts`）迁入后，包内用例的 `environmentRepo` / `core-bootstrap` 替身只能经宿主测试基建登记，因为宿主 preload 会把这些模块替换为转发 Proxy（见 review/task-1.5-host-aggregation.md §七）。三处都属「宿主测试基建的既有耦合」这一已登记形态，不是包对宿主生产实现的依赖。两条路径中只剩 §1.7 未完成，本条继续保留。 1.5g 复测（2026-09-21，工作树 872010416，2411 modules）据实改写复核：非测试侧仍为 **5 处 / 5 文件**（同上段列举的 5 个 `@server/db/schema` 表定义），未变化；测试侧 **18 处 / 18 文件**（`@server/test-utils/stubs/module-stubs` 16、`@server/plugins/error-handler` 1、`@server/db/schema` 1），比上段记载的 17 处 / 17 文件多 1 处，增量来自 1.5c-7（`6f39e4bdc`「core-bootstrap 拆分：兜底机器归 machine、实例收敛归 agent-runtime」）——`src/__tests__/machine-instance-cleanup.test.ts` 改经 `stubCoreBootstrap` 注入 core 单例替身，因而登记进宿主测试基建。该处与上段的三处同属「宿主测试基建的既有耦合」这一已登记形态（宿主 preload 把 `environmentRepo` / `core-bootstrap` 等替换为转发 Proxy，包内用例只能经 module-stubs 登记替身），不是包对宿主生产实现的依赖。 owner 于 2026-09-21 由 1.4 改挂 1.7（用户裁定）：1.4 §22.3 的结项表本就把本条指派给「1.7 表定义迁出 / 1.5 模块配置携带 baseUrl」，其中 1.5 的 baseUrl 半已随 1.5b 完成（见上段），台账 owner 只是收口时漏改；生产侧 5 处 `@server/db/schema` 确在 §1.7 范围内，但测试侧 18 处宿主测试基建耦合不随表定义迁出消失，故 §1.7 收口时须重测本条——若届时测试侧仍存在，按剩余事实改挂「未排期」或另立任务，不得视为已消化。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-machine",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 7 处导入 / 7 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。\n\n**（§1.7 B1 进度，2026-09-21）本包的 `machine` / `registry_event` 已迁至 `@fenix/resource-machine/db`**，包内 7 处引用的 5 处随之改指本包出口，宿主改经该出口取用；agent-config 与 agent-runtime 的调用期取数**不读表对象**，改为经宿主注入的端口（agent-config 的 `MachineLookupPort`、agent-runtime 的 `MachineRegistryPort`）——二者都不能直接导入本包：本包 `dependsOn` 已含 agent-config，反向声明会在模块装配期成环；sandbox 侧对 machine 表的唯一引用是一条列名断言（`machine.type.name`，属「资源包不该断言别包表结构」），随本批**删除**而非改指，属性存在性已在本包的 `registry-schema.test.ts` 接回，列名映射由 `check:schema-ddl-drift` 覆盖。残留 3 处、均为**跨模块表读取**：`src/server/services/registry.ts`（`agent_config`，owner agent-config）、`src/server/services/machine-sandbox-projection.ts`（`sandbox_instance`，owner sandbox）、`src/__tests__/registry-schema.test.ts`（`agent_config` 的外键列断言）。它们的目标表尚未迁出（分别在 §1.7 的 agent-config 批与 sandbox 批），本条目因此**必须保留到本包最后一个跨模块表读取消失为止**——台账是「包对」粒度，提前删除会连尚未迁出的读取一并放行。\n\n**（§1.7 B4 前置进度，2026-09-22）残留 3 处降为 2 处**：`sandbox_instance`（owner sandbox）的写入已按 §4.8 第 3 条裁定移到 sandbox 侧——本包不再直接 UPDATE 该表，改为经新增的 `machine-lifecycle-port.ts` 通报机器注册 / 心跳事件，由 `@fenix/resource-sandbox` 在 `createSandboxModule()` 里注入实现在自己的表上写（方向 sandbox → machine，与该包已有的 `MachineSandboxRoutePort` 同形，未绑定=正常降级），因此 `machine → sandbox` 的类别禁则违规面归零、无需新增依赖矩阵例外。`src/server/services/machine-sandbox-projection.ts` 与其测试已删除，新增 `src/server/machine-lifecycle-port.ts`。**剩余 2 处均为 `agent_config`（owner agent-config）**：`src/server/services/registry.ts` 的引用检查 / 绑定写、`src/__tests__/registry-schema.test.ts` 的外键列断言，随 §1.7 的 agent-config 批处理。实测生产侧 `from \"@server` 前缀命中 1 个文件（`src/server/services/registry.ts`）、包内 `sandbox_instance` 命中 5 行且全为注释 / 断言文本（生产代码 0 行）。本条目仍**必须保留到本包最后一个跨模块表读取消失为止**——台账是「包对」粒度，提前删除会连尚未迁出的读取一并放行。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/agent-config",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 11 处导入 / 11 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-workflow",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 4 处导入 / 4 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-knowledge",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 1 处导入 / 1 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/model-management",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`——1.5 侧的宿主能力面已随 1.5b 交付清空（`@server/db` 模块句柄与 `@server/config` 改经模块配置 / platform-sdk 受限入口）；B3 之后唯一成因是 `agent_config`（归 agent-config 批，`server/repositories/subject-agent-search.ts` 的只读检索），该表迁出后本包零 `@server/` 引用，届时删除本条。",
      "rationale": "1.5g 复测（2026-09-21，工作树 872010416，2411 modules）据实改写：实测 **6 处导入 / 6 个文件，全部是表定义**（`@server/db/schema`）——5 处生产（`server/access/provider-resource.ts`、`server/repositories/model-resource.ts`、`server/repositories/provider-resource.ts`、`server/repositories/model-gateway-credential.ts`、`server/repositories/subject-agent-search.ts`）+ 1 处测试（`src/__tests__/model-gateway-schema.test.ts`）。上次记载「12 处 / 9 个文件：6 处表定义 + 4 处宿主 db 模块 + 2 处宿主 config」，后两类 6 处已随 1.5b 整体归零（`server/db.ts` 与 `server/config.ts` 只剩解释性注释，不再有导入）。原 removeWhen 的前提「宿主中残留的资源领域实现迁回包内」不再成立：包内已不依赖任何宿主实现，故 owner 与同形态的 12 条一并归 §1.7、removeWhen 按其实际形态改写。\n\n**（§1.7 B3 进度，2026-09-21）本包的 `provider` / `model` / `model_gateway_credential` 已迁至 `@fenix/model-management/db`**（三个 pgEnum 随表迁入，是本仓 owner 包持有枚举的首例），包内 4 处生产引用与 1 处用例随之改指本包出口，宿主改经该出口取用以维持 `agent_config.model_id` 的外键表达；`drizzle.config.ts` 已声明该 schema 路径。残留由 6 处降为 **1 处**：`src/server/repositories/subject-agent-search.ts` 读的是**宿主自己的** `agent_config`（归 agent-config 批）。调用期的跨包取数不在本条范围内：`packages/resources/agent-config` 对 `model` / `provider` 的展示标签读取改为经宿主注入的 `ModelLookupPort`（本包 `dependsOn` 已含 agent-config，反向声明会闭合装配二元环，故不能像 B2 那样直连），端口实现是本包 `server/repositories/model-resource.ts` 的只读投影 `findModelLabelsByIds`，由宿主经本包 barrel 取用并绑定。本条目因此**必须保留到 agent-config 批落地为止**——台账是「包对」粒度，提前删除会把尚未迁出的读取一并放行。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-mcp",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`——1.5 侧的宿主能力面已随 1.5b 交付清空（`@server/db` 模块句柄与 `@server/config` 改经模块配置 / platform-sdk 受限入口）；B2 之后唯一成因是 `agent_config_mcp`（归 agent-config 批），该表迁出后本包零 `@server/` 引用，届时删除本条。",
      "rationale": "1.5g 复测（2026-09-21，工作树 872010416，2411 modules）据实改写：实测 **5 处导入 / 5 个文件，全部是表定义**（`@server/db/schema`）——4 处生产（`server/access/mcp-server-resource.ts`、`server/repositories/mcp-server.ts`、`server/services/mcp-server-service.ts`、`server/services/config/agent-config-mcp.ts`）+ 1 处测试（`src/__tests__/mcp-server-repository.test.ts`）。上次记载「6 处 / 6 个文件：5 处表定义 + 1 处宿主 auth 插件（宿主协议聚合，属 1.5 范围）」，那 1 处 `@server/plugins/auth` 已随 1.5b 的宿主守卫工厂化归零（`src/__tests__/guard-stubs.ts` 的注释记录了这次改口）。原 removeWhen 的前提「宿主中残留的资源领域实现迁回包内」不再成立：包内已不依赖任何宿主实现，故 owner 与同形态的 12 条一并归 §1.7、removeWhen 按其实际形态改写。\n\n**（§1.7 B2 进度，2026-09-21）本包的 `mcp_server` / `mcp_tool` 已迁至 `@fenix/resource-mcp/db`**，包内 4 处引用的 3 处生产文件与 1 处用例随之改指本包出口，宿主改经该出口取用以维持 `agent_config_mcp.mcp_server_id` 的外键表达；`drizzle.config.ts` 已声明该 schema 路径。残留由 5 处降为 **1 处**：`src/server/services/config/agent-config-mcp.ts` 读的是**宿主自己的** `agent_config_mcp` 关联表（该表归 agent-config 批，本包只是它的读写口径提供方）。调用期的跨包取数不在本条范围内：`packages/resources/agent-config` 对本包 `mcp_server` 的展示标签读取改为经本包 `./server/config` 出口的 `findMcpServerLabelsByIds`（本包 `dependsOn` 不含 agent-config，而 agent-config 已声明 `dependsOn: [\"mcp\"]`，方向合法，无需宿主注入端口）。本条目因此**必须保留到 agent-config 批落地为止**——台账是「包对」粒度，提前删除会把尚未迁出的读取一并放行。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-skill",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "关联表 `agent_config_skill` 的定义归属定夺并迁出（§1.7 B7）后，包不再引用 `@server/**`",
      "rationale": "实测 **1 处导入 / 1 个文件**（`repositories/agent-config-skill.ts` 读关联表 `agent_config_skill`）。B5（2026-09-22）已把 `skill` 资源行的表定义迁至 `@fenix/resource-skill/db`，原先的 3 处降为 1 处；剩下的这一处不是「本包自己的表没迁完」，而是 join 表的定义归属随评审文档 §8.4 第 8 条的 D4 复核（owner 定夺在 B7），故本条目按 §4.8 第 2 条保留至该批——提前删除会把尚未定夺的读取一并放行。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-task",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 6 处导入 / 6 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-observer",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 1 处导入 / 1 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-memory",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 1 处导入 / 1 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-prod-view",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 2 处导入 / 2 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    },
    {
      "rule": "apps-boundary",
      "from": "@fenix/resource-channel",
      "to": "@fenix/server-app",
      "owner": "1.7",
      "removeWhen": "表定义迁出（§1.7）后，包不再引用 `@server/**`",
      "rationale": "实测 1 处导入 / 1 个文件，全部为 `@server/db/schema` 表定义导入。宿主中的资源领域实现已随任务 1.3 迁回包内，残留仅剩表定义；表定义迁出归 §1.7，本条目保留至该步骤完成，不新建通配条目。"
    }
  ]
}
