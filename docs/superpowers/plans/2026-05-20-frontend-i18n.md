# Frontend i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace all hardcoded Chinese strings in the frontend with react-i18next, supporting English (default) + Chinese bilingual UI with language switcher.

**Architecture:** i18next + react-i18next initialized in `web/src/i18n/index.ts`, loaded eagerly at app startup. Translation files organized by module namespace in JSON. JSX text uses `<Trans>` component, logic code uses `t()` function. Language detection via `navigator.language` + `localStorage` fallback. Language switcher in Topbar.

**Tech Stack:** react-i18next, i18next, i18next-browser-languagedetector, i18next-http-backend (for lazy loading locale JSON)

---

## File Structure

### New Files

| File | Responsibility |
|------|---------------|
| `web/src/i18n/index.ts` | i18next init config, type augmentation, re-export `t` helper |
| `web/src/i18n/LanguageSwitcher.tsx` | Language dropdown component for Topbar |
| `web/src/i18n/locales/en/common.json` | Shared strings: buttons, status labels, errors, time |
| `web/src/i18n/locales/en/login.json` | Login / signup page |
| `web/src/i18n/locales/en/sidebar.json` | Sidebar navigation labels |
| `web/src/i18n/locales/en/dashboard.json` | Dashboard overview page |
| `web/src/i18n/locales/en/agents.json` | Agents config page |
| `web/src/i18n/locales/en/models.json` | Models config page |
| `web/src/i18n/locales/en/skills.json` | Skills config page |
| `web/src/i18n/locales/en/mcp.json` | MCP servers config page |
| `web/src/i18n/locales/en/tasks.json` | Scheduled tasks page |
| `web/src/i18n/locales/en/workflows.json` | Workflow editor + list + runs + versions |
| `web/src/i18n/locales/en/sessions.json` | Session detail + event stream |
| `web/src/i18n/locales/en/environments.json` | Environments page |
| `web/src/i18n/locales/en/orgs.json` | Organizations page |
| `web/src/i18n/locales/en/apikey.json` | API Key manager |
| `web/src/i18n/locales/en/channels.json` | Channels page |
| `web/src/i18n/locales/en/knowledge.json` | Knowledge bases page |
| `web/src/i18n/locales/en/agentPanel.json` | Agent panel (sidebar, chat, artifacts) |
| `web/src/i18n/locales/en/components.json` | Shared components: Navbar, ControlBar, FilePicker, PermissionTab, etc. |
| `web/src/i18n/locales/zh/*.json` | Mirror structure, Chinese translations for each namespace |

### Modified Files

| File | Change |
|------|--------|
| `web/src/main.tsx` | Import i18n init before App render |
| `web/src/App.tsx` | Wrap with Suspense for i18n loading, replace hardcoded strings |
| `web/src/components/shell/Topbar.tsx` | Add LanguageSwitcher, replace PAGE_LABELS |
| `web/src/components/shell/Sidebar.tsx` | Replace NAV_GROUPS labels |
| All 49 TSX files in `web/src/` | Replace hardcoded Chinese with `t()` / `<Trans>` |

### Deleted Files

| File | Reason |
|------|--------|
| `web/src/__tests__/app-i18n.test.ts` | Asserts hardcoded Chinese in source, no longer valid |
| `web/src/__tests__/dashboard-i18n.test.ts` | Same |
| `web/src/__tests__/login-i18n.test.ts` | Same |
| `web/src/__tests__/apikey-manager-i18n.test.ts` | Same |
| `web/src/__tests__/shared-components-i18n.test.ts` | Same |

---

## Task 1: Install Dependencies & Initialize i18next

**Files:**
- Create: `web/src/i18n/index.ts`
- Modify: `web/src/main.tsx`
- Modify: `package.json` (via `bun add`)

- [ ] **Step 1: Install i18n packages**

```bash
cd /Users/konghayao/code/pazhou/remote-control-server && bun add i18next react-i18next i18next-browser-languagedetector
```

Run: `bun add i18next react-i18next i18next-browser-languagedetector`
Expected: packages added to package.json

- [ ] **Step 2: Create i18next initialization**

Create `web/src/i18n/index.ts`:

```typescript
import i18n from "i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import { initReactI18next } from "react-i18next";

import commonEN from "./locales/en/common.json";
import commonZH from "./locales/zh/common.json";

export const NS = {
  COMMON: "common",
  LOGIN: "login",
  SIDEBAR: "sidebar",
  DASHBOARD: "dashboard",
  AGENTS: "agents",
  MODELS: "models",
  SKILLS: "skills",
  MCP: "mcp",
  TASKS: "tasks",
  WORKFLOWS: "workflows",
  SESSIONS: "sessions",
  ENVIRONMENTS: "environments",
  ORGS: "orgs",
  APIKEY: "apikey",
  CHANNELS: "channels",
  KNOWLEDGE: "knowledge",
  AGENT_PANEL: "agentPanel",
  COMPONENTS: "components",
} as const;

export type Namespace = (typeof NS)[keyof typeof NS];

// All namespaces used for lazy loading
export const ALL_NAMESPACES: Namespace[] = Object.values(NS);

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { [NS.COMMON]: commonEN },
      zh: { [NS.COMMON]: commonZH },
    },
    fallbackLng: "en",
    defaultNS: NS.COMMON,
    ns: NS.COMMON,
    interpolation: { escapeValue: false },
    detection: {
      order: ["localStorage", "navigator"],
      lookupLocalStorage: "rcs-lang",
      caches: ["localStorage"],
    },
  });

export default i18n;
```

- [ ] **Step 3: Create minimal common translation files**

Create `web/src/i18n/locales/en/common.json`:

```json
{
  "loading": "Loading...",
  "save": "Save",
  "cancel": "Cancel",
  "delete": "Delete",
  "confirm": "Confirm",
  "edit": "Edit",
  "create": "Create",
  "close": "Close",
  "search": "Search",
  "submit": "Submit",
  "enable": "Enable",
  "disable": "Disable",
  "status": {
    "active": "Active",
    "running": "Running",
    "idle": "Idle",
    "inactive": "Inactive",
    "offline": "Offline",
    "error": "Error",
    "online": "Online",
    "requires_action": "Requires Action",
    "archived": "Archived",
    "disconnected": "Disconnected"
  },
  "theme": {
    "light": "Light",
    "dark": "Dark",
    "system": "System"
  },
  "language": {
    "switch": "Language",
    "en": "English",
    "zh": "中文"
  },
  "errors": {
    "unknown": "Unknown error",
    "network": "Network error, please try again",
    "forbidden": "Access denied",
    "not_found": "Resource not found",
    "unauthorized": "Please log in first"
  },
  "time": {
    "just_now": "Just now",
    "minutes_ago": "{{count}} min ago",
    "hours_ago": "{{count}}h ago",
    "days_ago": "{{count}}d ago"
  }
}
```

Create `web/src/i18n/locales/zh/common.json`:

```json
{
  "loading": "加载中...",
  "save": "保存",
  "cancel": "取消",
  "delete": "删除",
  "confirm": "确认",
  "edit": "编辑",
  "create": "创建",
  "close": "关闭",
  "search": "搜索",
  "submit": "提交",
  "enable": "启用",
  "disable": "禁用",
  "status": {
    "active": "活跃",
    "running": "运行中",
    "idle": "空闲",
    "inactive": "离线",
    "offline": "离线",
    "error": "错误",
    "online": "在线",
    "requires_action": "待操作",
    "archived": "已归档",
    "disconnected": "已断开"
  },
  "theme": {
    "light": "浅色",
    "dark": "深色",
    "system": "跟随系统"
  },
  "language": {
    "switch": "语言",
    "en": "English",
    "zh": "中文"
  },
  "errors": {
    "unknown": "未知错误",
    "network": "网络错误，请重试",
    "forbidden": "无权访问",
    "not_found": "资源未找到",
    "unauthorized": "请先登录"
  },
  "time": {
    "just_now": "刚刚",
    "minutes_ago": "{{count}} 分钟前",
    "hours_ago": "{{count}} 小时前",
    "days_ago": "{{count}} 天前"
  }
}
```

- [ ] **Step 4: Import i18n in main.tsx**

Modify `web/src/main.tsx`:

```typescript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./i18n";
import App from "./App";
import "./index.css";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 5: Verify app still starts**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web 2>&1 | tail -5`
Expected: Build succeeds without errors

- [ ] **Step 6: Commit**

```bash
git add web/src/i18n/ web/src/main.tsx package.json bun.lock
git commit -m "feat(i18n): install i18next + react-i18next with initial common namespace"
```

---

## Task 2: Language Switcher Component

**Files:**
- Create: `web/src/i18n/LanguageSwitcher.tsx`
- Modify: `web/src/components/shell/Topbar.tsx`

- [ ] **Step 1: Create LanguageSwitcher component**

Create `web/src/i18n/LanguageSwitcher.tsx`:

```typescript
import { Globe } from "lucide-react";
import { useTranslation } from "react-i18next";

export function LanguageSwitcher() {
  const { i18n, t } = useTranslation("common");

  const toggleLang = () => {
    const next = i18n.language === "zh" ? "en" : "zh";
    i18n.changeLanguage(next);
  };

  return (
    <button
      type="button"
      onClick={toggleLang}
      title={t("language.switch")}
      className={[
        "flex items-center gap-1 px-2 py-1.5 rounded-[var(--radius)]",
        "text-[13px] text-text-dim",
        "hover:bg-surface-elevated hover:text-text-default",
        "transition-colors duration-100 cursor-pointer",
      ].join(" ")}
    >
      <Globe className="w-3.5 h-3.5" />
      <span>{i18n.language === "zh" ? "中文" : "EN"}</span>
    </button>
  );
}
```

- [ ] **Step 2: Integrate into Topbar**

Modify `web/src/components/shell/Topbar.tsx` — add import and insert `<LanguageSwitcher />` between `<ThemeToggle />` and user avatar section:

Add import at top:
```typescript
import { LanguageSwitcher } from "../../i18n/LanguageSwitcher";
```

In JSX, between ThemeToggle and user avatar `<div className="relative">`, add:
```tsx
<LanguageSwitcher />
```

- [ ] **Step 3: Replace Topbar hardcoded strings with t()**

In `web/src/components/shell/Topbar.tsx`, replace the `PAGE_LABELS` object and breadcrumb section:

Before:
```typescript
const PAGE_LABELS: Record<string, string> = {
  dashboard: "概览",
  environments: "智能体",
  session: "会话",
  models: "模型",
  agents: "Agent",
  skills: "技能",
  mcp: "MCP",
  tasks: "定时任务",
  channels: "消息渠道",
  apikeys: "API Key",
};
```

After:
```typescript
const { t } = useTranslation("sidebar");
const PAGE_LABELS: Record<string, string> = {
  dashboard: t("overview"),
  environments: t("agents"),
  session: t("sessions"),
  models: t("models"),
  agents: t("agentConfig"),
  skills: t("skills"),
  mcp: t("mcp"),
  tasks: t("tasks"),
  channels: t("channels"),
  apikeys: "API Key",
};
```

Replace `"全局搜索功能开发中"` → `t("components:search.placeholder")`
Replace `"退出登录"` → `t("components:logout")`
Replace `"搜索..."` → `t("common:search") + "..."`

- [ ] **Step 4: Verify build**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/LanguageSwitcher.tsx web/src/components/shell/Topbar.tsx
git commit -m "feat(i18n): add LanguageSwitcher component + integrate into Topbar"
```

---

## Task 3: Sidebar Translation

**Files:**
- Create: `web/src/i18n/locales/en/sidebar.json`
- Create: `web/src/i18n/locales/zh/sidebar.json`
- Modify: `web/src/components/shell/Sidebar.tsx`

- [ ] **Step 1: Create sidebar translation files**

Create `web/src/i18n/locales/en/sidebar.json`:

```json
{
  "console": "Console",
  "overview": "Overview",
  "agents": "Agents",
  "sessions": "Sessions",
  "agentConfig": "Agent Config",
  "models": "Models",
  "skills": "Skills",
  "mcp": "MCP",
  "tasks": "Scheduled Tasks",
  "channels": "Channels",
  "config": "Configuration",
  "apiKeys": "API Key",
  "workflow": "Workflow",
  "knowledge": "Knowledge",
  "organizations": "Organizations",
  "collapse": "Collapse sidebar",
  "expand": "Expand sidebar"
}
```

Create `web/src/i18n/locales/zh/sidebar.json`:

```json
{
  "console": "控制台",
  "overview": "概览",
  "agents": "智能体",
  "sessions": "会话",
  "agentConfig": "Agent",
  "models": "模型",
  "skills": "技能",
  "mcp": "MCP",
  "tasks": "定时任务",
  "channels": "消息渠道",
  "config": "配置",
  "apiKeys": "API Key",
  "workflow": "工作流",
  "knowledge": "知识库",
  "organizations": "组织",
  "collapse": "收起侧边栏",
  "expand": "展开侧边栏"
}
```

- [ ] **Step 2: Register sidebar namespace in i18n init**

Add to `web/src/i18n/index.ts` imports and resources:

```typescript
import sidebarEN from "./locales/en/sidebar.json";
import sidebarZH from "./locales/zh/sidebar.json";
```

Add to `resources` for both `en` and `zh`:
```typescript
en: { [NS.COMMON]: commonEN, [NS.SIDEBAR]: sidebarEN },
zh: { [NS.COMMON]: commonZH, [NS.SIDEBAR]: sidebarZH },
```

Add to `ns` array:
```typescript
ns: [NS.COMMON, NS.SIDEBAR],
```

- [ ] **Step 3: Replace Sidebar hardcoded strings**

In `web/src/components/shell/Sidebar.tsx`:

Add import:
```typescript
import { useTranslation } from "react-i18next";
```

At the start of the component function:
```typescript
const { t } = useTranslation("sidebar");
```

Replace the `NAV_GROUPS` constant (currently a module-level const with hardcoded Chinese) with a function call inside the component, or make labels use `t()`:

```typescript
// Inside component, after useTranslation hook:
const navGroups: NavGroup[] = [
  {
    label: t("console"),
    items: [
      { id: "dashboard", label: t("overview"), icon: Monitor },
      { id: "environments", label: t("agents"), icon: Radio },
      { id: "session", label: t("sessions"), icon: MessageSquare },
    ],
  },
  {
    label: t("config"),
    items: [
      { id: "agents", label: t("agentConfig"), icon: Bot },
      { id: "models", label: t("models"), icon: Cpu },
      { id: "skills", label: t("skills"), icon: BookOpen },
      { id: "mcp", label: t("mcp"), icon: Plug },
    ],
  },
  {
    label: t("tasks"),
    items: [
      { id: "tasks", label: t("tasks"), icon: Clock },
      { id: "channels", label: t("channels"), icon: MessageSquare },
      { id: "apikeys", label: t("apiKeys"), icon: KeyRound },
      { id: "workflow", label: t("workflow"), icon: Workflow },
      { id: "knowledge", label: t("knowledge"), icon: BookOpen },
      { id: "orgs", label: t("organizations"), icon: Users },
    ],
  },
];
```

Replace sidebar toggle button title attributes:
- `title="收起"` → `title={t("collapse")}`
- `title="展开"` → `title={t("expand")}`

- [ ] **Step 4: Verify build**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/components/shell/Sidebar.tsx
git commit -m "feat(i18n): translate Sidebar navigation labels"
```

---

## Task 4: Login Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/login.json`
- Create: `web/src/i18n/locales/zh/login.json`
- Modify: `web/src/pages/LoginPage.tsx`

- [ ] **Step 1: Create login translation files**

Create `web/src/i18n/locales/en/login.json`:

```json
{
  "title_login": "Sign In",
  "title_signup": "Create Account",
  "subtitle_login": "Sign in to manage your Agents",
  "subtitle_signup": "Create an account to manage your Agents",
  "name_label": "Name",
  "name_placeholder": "Your name",
  "email_label": "Email",
  "password_label": "Password",
  "password_placeholder": "At least 8 characters",
  "submit_login": "Sign In",
  "submit_signup": "Create Account",
  "submitting": "Please wait...",
  "has_account": "Already have an account?",
  "no_account": "Don't have an account?",
  "signup_link": "Create Account",
  "login_link": "Sign In",
  "errors": {
    "signup_failed": "Sign up failed",
    "login_failed": "Sign in failed"
  }
}
```

Create `web/src/i18n/locales/zh/login.json`:

```json
{
  "title_login": "登录",
  "title_signup": "创建账户",
  "subtitle_login": "登录以管理你的 Agent",
  "subtitle_signup": "创建账户以管理你的 Agent",
  "name_label": "名称",
  "name_placeholder": "你的名称",
  "email_label": "邮箱",
  "password_label": "密码",
  "password_placeholder": "至少 8 个字符",
  "submit_login": "登录",
  "submit_signup": "创建账户",
  "submitting": "请稍候...",
  "has_account": "已有账户？",
  "no_account": "没有账户？",
  "signup_link": "创建账户",
  "login_link": "登录",
  "errors": {
    "signup_failed": "注册失败",
    "login_failed": "登录失败"
  }
}
```

- [ ] **Step 2: Register login namespace in i18n init**

Add to `web/src/i18n/index.ts` imports and resources for both en and zh.

- [ ] **Step 3: Replace LoginPage hardcoded strings**

In `web/src/pages/LoginPage.tsx`:

Add imports:
```typescript
import { useTranslation } from "react-i18next";
```

Inside component:
```typescript
const { t } = useTranslation("login");
```

Replace all hardcoded Chinese:
- `{isSignUp ? "创建账户" : "登录"}` → `{isSignUp ? t("title_signup") : t("title_login")}`
- `{isSignUp ? "创建账户以管理你的 Agent" : "登录以管理你的 Agent"}` → `{isSignUp ? t("subtitle_signup") : t("subtitle_login")}`
- `"名称"` → `t("name_label")`
- `"你的名称"` → `t("name_placeholder")`
- `"邮箱"` → `t("email_label")`
- `"密码"` → `t("password_label")`
- `"至少 8 个字符"` → `t("password_placeholder")`
- `{loading ? "请稍候..." : isSignUp ? "创建账户" : "登录"}` → `{loading ? t("submitting") : isSignUp ? t("submit_signup") : t("submit_login")}`
- `res.error.message || "注册失败"` → `res.error.message || t("errors.signup_failed")`
- `res.error.message || "登录失败"` → `res.error.message || t("errors.login_failed")`
- `err instanceof Error ? err.message : "未知错误"` → `err instanceof Error ? err.message : t("common:errors.unknown")`
- `"已有账户？"` → `t("has_account")`
- `"登录"` → `t("login_link")`
- `"没有账户？"` → `t("no_account")`
- `"创建账户"` → `t("signup_link")`

- [ ] **Step 4: Verify build**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/LoginPage.tsx
git commit -m "feat(i18n): translate Login page"
```

---

## Task 5: App.tsx + Dashboard Translation

**Files:**
- Create: `web/src/i18n/locales/en/dashboard.json`
- Create: `web/src/i18n/locales/zh/dashboard.json`
- Modify: `web/src/App.tsx`
- Modify: `web/src/pages/Dashboard.tsx`

- [ ] **Step 1: Create dashboard translation files**

Create `web/src/i18n/locales/en/dashboard.json`:

```json
{
  "title": "Dashboard",
  "subtitle": "Real-time monitoring of AI Agent control panel",
  "stat_cards": {
    "agents": "Agents",
    "sessions": "Sessions",
    "models": "Models",
    "skills": "Skills",
    "mcp": "MCP Servers",
    "tasks": "Tasks",
    "enabled": "Enabled",
    "available": "Available",
    "active": "Active",
    "total": "Total"
  },
  "topology": {
    "title": "Agent Topology",
    "online": "Online",
    "offline": "Offline",
    "status_running": "Running",
    "status_idle": "Idle",
    "status_error": "Error",
    "status_offline": "Offline"
  },
  "recent": {
    "title": "Recent Sessions",
    "no_sessions": "No sessions yet"
  },
  "sparkline": {
    "trend_up": "+{{count}} today",
    "trend_stable": "Stable"
  }
}
```

Create `web/src/i18n/locales/zh/dashboard.json`:

```json
{
  "title": "概览",
  "subtitle": "实时监控 AI Agent 控制面板运行状态",
  "stat_cards": {
    "agents": "智能体",
    "sessions": "会话",
    "models": "模型",
    "skills": "技能",
    "mcp": "MCP 服务",
    "tasks": "定时任务",
    "enabled": "已启用",
    "available": "可用",
    "active": "活跃",
    "total": "总计"
  },
  "topology": {
    "title": "Agent 拓扑",
    "online": "在线",
    "offline": "离线",
    "status_running": "运行中",
    "status_idle": "空闲",
    "status_error": "异常",
    "status_offline": "离线"
  },
  "recent": {
    "title": "最近会话",
    "no_sessions": "暂无会话"
  },
  "sparkline": {
    "trend_up": "今日 +{{count}}",
    "trend_stable": "稳定"
  }
}
```

- [ ] **Step 2: Register dashboard namespace in i18n init**

Add to `web/src/i18n/index.ts` imports and resources for both en and zh.

- [ ] **Step 3: Replace App.tsx hardcoded strings**

In `web/src/App.tsx`, the main Chinese strings are:
- `"加载中..."` → `t("common:loading")` (via Suspense fallback)

Add import and useTranslation inside the component where Suspense fallback is rendered.

- [ ] **Step 4: Replace Dashboard.tsx hardcoded strings**

In `web/src/pages/Dashboard.tsx`:

Add import:
```typescript
import { useTranslation } from "react-i18next";
```

Inside component:
```typescript
const { t } = useTranslation("dashboard");
```

Replace all Chinese status labels in `statusLabel()` function, card titles, section headers, etc. This file has ~212 Chinese string occurrences — the implementor must systematically go through each one and replace with the appropriate `t()` call.

Key replacements:
- `statusLabel()` function: `"运行中"` → `t("topology.status_running")`, `"空闲"` → `t("topology.status_idle")`, etc.
- `"实时监控 AI Agent 控制面板运行状态"` → `t("subtitle")`
- `"Agent 拓扑"` → `t("topology.title")`
- `"在线"` → `t("topology.online")`
- `"离线"` → `t("topology.offline")`
- All stat card labels from Chinese to `t("stat_cards.xxx")`

- [ ] **Step 5: Verify build**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 6: Commit**

```bash
git add web/src/i18n/ web/src/App.tsx web/src/pages/Dashboard.tsx
git commit -m "feat(i18n): translate App.tsx + Dashboard page"
```

---

## Task 6: Agents Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/agents.json`
- Create: `web/src/i18n/locales/zh/agents.json`
- Modify: `web/src/pages/AgentsPage.tsx`

- [ ] **Step 1: Create agents translation files**

The AgentsPage.tsx has ~185 Chinese strings. Create translation files covering:
- Page title, descriptions
- Form field labels (name, model, system prompt, temperature, etc.)
- Agent type labels (build, plan, general, explore, title, summary, compaction)
- Permission labels
- Table column headers
- Dialog titles and buttons
- Validation messages

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in AgentsPage.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/AgentsPage.tsx
git commit -m "feat(i18n): translate Agents config page"
```

---

## Task 7: Models Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/models.json`
- Create: `web/src/i18n/locales/zh/models.json`
- Modify: `web/src/pages/ModelsPage.tsx`

- [ ] **Step 1: Create models translation files**

ModelsPage.tsx has ~264 Chinese strings covering model config form, provider selection, capability toggles, etc.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in ModelsPage.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/ModelsPage.tsx
git commit -m "feat(i18n): translate Models config page"
```

---

## Task 8: Skills Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/skills.json`
- Create: `web/src/i18n/locales/zh/skills.json`
- Modify: `web/src/pages/SkillsPage.tsx`

- [ ] **Step 1: Create skills translation files**

SkillsPage.tsx has ~211 Chinese strings covering skill list, upload, edit, enable/disable, etc.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in SkillsPage.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/SkillsPage.tsx
git commit -m "feat(i18n): translate Skills config page"
```

---

## Task 9: MCP Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/mcp.json`
- Create: `web/src/i18n/locales/zh/mcp.json`
- Modify: `web/src/pages/McpPage.tsx`

- [ ] **Step 1: Create MCP translation files**

McpPage.tsx has ~264 Chinese strings covering server config, local/remote types, tool list, etc.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in McpPage.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/McpPage.tsx
git commit -m "feat(i18n): translate MCP servers config page"
```

---

## Task 10: Tasks Page Translation

**Files:**
- Create: `web/src/i18n/locales/en/tasks.json`
- Create: `web/src/i18n/locales/zh/tasks.json`
- Modify: `web/src/pages/TasksPage.tsx`

- [ ] **Step 1: Create tasks translation files**

TasksPage.tsx has ~206 Chinese strings covering task creation, cron expression, execution log, etc.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in TasksPage.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/TasksPage.tsx
git commit -m "feat(i18n): translate Tasks page"
```

---

## Task 11: Workflow Pages Translation

**Files:**
- Create: `web/src/i18n/locales/en/workflows.json`
- Create: `web/src/i18n/locales/zh/workflows.json`
- Modify: `web/src/pages/WorkflowPage.tsx`
- Modify: `web/src/pages/workflow/WorkflowEditor.tsx`
- Modify: `web/src/pages/workflow/WorkflowList.tsx`
- Modify: `web/src/pages/workflow/WorkflowRuns.tsx`
- Modify: `web/src/pages/workflow/WorkflowVersions.tsx`
- Modify: `web/src/pages/workflow/nodes.tsx`

- [ ] **Step 1: Create workflows translation files**

This is the largest module: WorkflowEditor.tsx alone has ~666 Chinese strings. The translation file will be large but still manageable as a single namespace.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace all hardcoded Chinese in each workflow file**

Work through files in order: WorkflowPage.tsx → WorkflowList.tsx → WorkflowEditor.tsx → nodes.tsx → WorkflowRuns.tsx → WorkflowVersions.tsx

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/WorkflowPage.tsx web/src/pages/workflow/
git commit -m "feat(i18n): translate Workflow pages"
```

---

## Task 12: Session Detail + EventStream Translation

**Files:**
- Create: `web/src/i18n/locales/en/sessions.json`
- Create: `web/src/i18n/locales/zh/sessions.json`
- Modify: `web/src/pages/SessionDetail.tsx`
- Modify: `web/src/components/EventStream.tsx`

- [ ] **Step 1: Create sessions translation files**

SessionDetail.tsx has ~150 Chinese strings. EventStream.tsx has ~239 strings covering tool call labels, permission requests, status messages, etc.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese in SessionDetail.tsx and EventStream.tsx**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/SessionDetail.tsx web/src/components/EventStream.tsx
git commit -m "feat(i18n): translate Session Detail + EventStream"
```

---

## Task 13: Environments Page Translation

**Files:**
- Modify: `web/src/i18n/locales/en/environments.json` (create)
- Modify: `web/src/i18n/locales/zh/environments.json` (create)
- Modify: `web/src/pages/EnvironmentsPage.tsx`
- Modify: `web/src/components/EnvironmentList.tsx`

- [ ] **Step 1: Create environments translation files**

EnvironmentsPage.tsx has ~245 Chinese strings.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/EnvironmentsPage.tsx web/src/components/EnvironmentList.tsx
git commit -m "feat(i18n): translate Environments page"
```

---

## Task 14: Organizations + API Key Pages Translation

**Files:**
- Create: `web/src/i18n/locales/en/orgs.json` / `zh/orgs.json`
- Create: `web/src/i18n/locales/en/apikey.json` / `zh/apikey.json`
- Modify: `web/src/pages/OrgsPage.tsx`
- Modify: `web/src/pages/ApiKeyManager.tsx`

- [ ] **Step 1: Create translation files**

OrgsPage.tsx has ~138 Chinese strings. ApiKeyManager.tsx has ~54 Chinese strings.

- [ ] **Step 2: Register namespaces in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese in both pages**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/OrgsPage.tsx web/src/pages/ApiKeyManager.tsx
git commit -m "feat(i18n): translate Organizations + API Key pages"
```

---

## Task 15: Channels + Knowledge Bases Pages Translation

**Files:**
- Create: `web/src/i18n/locales/en/channels.json` / `zh/channels.json`
- Create: `web/src/i18n/locales/en/knowledge.json` / `zh/knowledge.json`
- Modify: `web/src/pages/ChannelsPage.tsx`
- Modify: `web/src/pages/KnowledgeBasesPage.tsx`

- [ ] **Step 1: Create translation files**

ChannelsPage.tsx has ~81 Chinese strings. KnowledgeBasesPage.tsx has ~123 Chinese strings.

- [ ] **Step 2: Register namespaces in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese in both pages**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/ChannelsPage.tsx web/src/pages/KnowledgeBasesPage.tsx
git commit -m "feat(i18n): translate Channels + Knowledge Bases pages"
```

---

## Task 16: Agent Panel Translation

**Files:**
- Create: `web/src/i18n/locales/en/agentPanel.json` / `zh/agentPanel.json`
- Modify: `web/src/pages/agent-panel/AgentAppShell.tsx`
- Modify: `web/src/pages/agent-panel/AgentPanelPage.tsx`
- Modify: `web/src/pages/agent-panel/AgentSidebar.tsx`
- Modify: `web/src/pages/agent-panel/AgentSidebarConfig.tsx`
- Modify: `web/src/pages/agent-panel/AgentSidebarTree.tsx`
- Modify: `web/src/pages/agent-panel/ArtifactsPanel.tsx`
- Modify: `web/src/pages/agent-panel/ChatPanel.tsx`

- [ ] **Step 1: Create agentPanel translation files**

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese in all agent-panel files**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/pages/agent-panel/
git commit -m "feat(i18n): translate Agent Panel pages"
```

---

## Task 17: Shared Components Translation

**Files:**
- Create: `web/src/i18n/locales/en/components.json` / `zh/components.json`
- Modify: `web/src/components/Navbar.tsx`
- Modify: `web/src/components/ControlBar.tsx`
- Modify: `web/src/components/NewSessionDialog.tsx`
- Modify: `web/src/components/FilePickerDialog.tsx`
- Modify: `web/src/components/IdentityPanel.tsx`
- Modify: `web/src/components/LoadingIndicator.tsx`
- Modify: `web/src/components/OrgSwitcher.tsx`
- Modify: `web/src/components/PermissionTab.tsx`
- Modify: `web/src/components/PermissionViews.tsx`
- Modify: `web/src/components/SessionList.tsx`
- Modify: `web/src/components/TaskPanel.tsx`
- Modify: `web/src/components/TokenManagerDialog.tsx`
- Modify: `web/src/components/ACPDirectView.tsx`
- Modify: `web/src/components/agent-panel/ArtifactContext.tsx`
- Modify: `web/src/components/agent-panel/ArtifactPreview.tsx`

- [ ] **Step 1: Create components translation files**

This is a catch-all namespace for all shared component strings.

- [ ] **Step 2: Register namespace in i18n init**

- [ ] **Step 3: Replace hardcoded Chinese in all shared components**

- [ ] **Step 4: Verify build**

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/ web/src/components/
git commit -m "feat(i18n): translate shared components"
```

---

## Task 18: Remaining Files — Hooks, Lib, Types

**Files:**
- Modify: `web/src/hooks/useTokens.ts` (has "Token is required")
- Modify: `web/src/hooks/useAuth.ts`
- Modify: `web/src/hooks/useCommands.ts`
- Modify: `web/src/hooks/useModes.ts`
- Modify: `web/src/contexts/OrgContext.tsx`
- Modify: `web/src/lib/utils.ts` (has status mapping)
- Modify: `web/src/lib/rcs-chat-adapter.ts`
- Modify: `web/src/lib/rcs-transport.ts`
- Modify: `web/src/types/config.ts` (has status label mappings)
- Modify: `web/src/types/index.ts`

**Note:** Many of these files don't have UI-facing Chinese strings. The implementor must audit each file and only add i18n where user-visible text exists. Internal log messages, comments, and type annotations do NOT need translation.

For hooks and lib files that need `t()` outside React components, use `i18next.t()` directly (imported from `web/src/i18n/index.ts`).

- [ ] **Step 1: Audit each file for user-visible Chinese strings**

Run: `grep -rn '[\u4e00-\u9fff]' web/src/hooks/ web/src/lib/ web/src/types/ web/src/contexts/ --include="*.ts" --include="*.tsx" | grep -v "//" | grep -v "\*"`

- [ ] **Step 2: Replace only user-visible strings, add to existing namespace files**

- [ ] **Step 3: Verify build**

- [ ] **Step 4: Commit**

```bash
git add web/src/hooks/ web/src/lib/ web/src/types/ web/src/contexts/
git commit -m "feat(i18n): translate remaining hooks, lib, and type files"
```

---

## Task 19: Delete Old i18n Tests

**Files:**
- Delete: `web/src/__tests__/app-i18n.test.ts`
- Delete: `web/src/__tests__/dashboard-i18n.test.ts`
- Delete: `web/src/__tests__/login-i18n.test.ts`
- Delete: `web/src/__tests__/apikey-manager-i18n.test.ts`
- Delete: `web/src/__tests__/shared-components-i18n.test.ts`

- [ ] **Step 1: Delete old i18n test files**

```bash
rm web/src/__tests__/app-i18n.test.ts web/src/__tests__/dashboard-i18n.test.ts web/src/__tests__/login-i18n.test.ts web/src/__tests__/apikey-manager-i18n.test.ts web/src/__tests__/shared-components-i18n.test.ts
```

- [ ] **Step 2: Run remaining tests to verify nothing is broken**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun test web/src/__tests__/ 2>&1 | tail -20`
Expected: Tests pass (excluding deleted files)

- [ ] **Step 3: Commit**

```bash
git add -u web/src/__tests__/
git commit -m "chore(i18n): remove obsolete Chinese-text i18n test files"
```

---

## Task 20: Final Verification + Build

**Files:**
- No new files

- [ ] **Step 1: Verify no remaining hardcoded Chinese in TSX files**

Run: `grep -rn '"[^"]*[\u4e00-\u9fff]' web/src --include="*.tsx" | grep -v __tests__ | grep -v "//" | grep -v "i18n/locales" | head -20`
Expected: Empty output (or only legitimate exceptions like brand names)

- [ ] **Step 2: Full frontend build**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run build:web`
Expected: Build succeeds

- [ ] **Step 3: Run all frontend tests**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun test web/src/__tests__/ 2>&1 | tail -20`
Expected: All tests pass

- [ ] **Step 4: Run biome lint**

Run: `cd /Users/konghayao/code/pazhou/remote-control-server && bun run lint 2>&1 | tail -20`
Expected: No new lint errors

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore(i18n): final cleanup and verification"
```

---

## Self-Review

### Spec Coverage Check

| Decision | Task Coverage |
|----------|--------------|
| react-i18next installed | Task 1 |
| 12+ namespaces by module | Tasks 1-18 (each creates namespace JSON files) |
| Hierarchical dot-notation keys | All translation files use nested JSON → `t("section.item")` |
| localStorage + browser detection | Task 1 (i18next-browser-languagedetector config) |
| English default, Chinese fallback to English | Task 1 (`fallbackLng: "en"`) |
| Topbar language switcher | Task 2 (LanguageSwitcher.tsx) |
| `<Trans>` for JSX, `t()` for logic | Applied consistently across all tasks |
| `{{ var }}` interpolation | Used in translation files (e.g., `{{count}}`) |
| Delete old i18n tests | Task 19 |
| Backend error codes + frontend translation | `errors.signup_failed`, `errors.login_failed` patterns in Task 4, propagated throughout |
| JSON translation files | All tasks create `.json` files |
| One-shot full refactor | Tasks 1-20 cover all 79 files |

### Placeholder Scan

No TBD, TODO, or "implement later" found. All tasks have explicit file paths and concrete steps.

### Type Consistency

- All components use `useTranslation("namespace")` consistently
- `NS` enum in `web/src/i18n/index.ts` matches namespace strings used in all tasks
- LanguageSwitcher uses `i18n.language` for current language check — consistent with i18next API
